services:
  postgres:
    image: postgres:13
    container_name: aagdoom-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: aagdoom
      POSTGRES_PASSWORD: aagdoom
      POSTGRES_DB: aagdoom
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aagdoom -d aagdoom"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: aagdoom-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
  redis_data: